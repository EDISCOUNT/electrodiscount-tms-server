{% extends 'base.html.twig' %}

{% block title %}Carrier index{% endblock %}

{% block body %}
    <h1>Carrier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Name</th>
                <th>Enabled</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carrier in carriers %}
            <tr>
                <td>{{ carrier.id }}</td>
                <td>{{ carrier.code }}</td>
                <td>{{ carrier.name }}</td>
                <td>{{ carrier.enabled ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_api_admin_carrier_carrier_show', {'id': carrier.id}) }}">show</a>
                    <a href="{{ path('app_api_admin_carrier_carrier_edit', {'id': carrier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_api_admin_carrier_carrier_new') }}">Create new</a>
{% endblock %}
